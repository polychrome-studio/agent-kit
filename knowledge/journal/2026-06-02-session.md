---
date: 2026-06-02
session: session
status: in-progress
---

# Session — 2026-06-02

> Auto-breadcrumbs from commits below. Flesh out the **why** under each while fresh.

## Context
Tucker live-confirmed the agent loop + web fix work (web_search now returns real results;
a web search even ran under Sonnet/companion and he was happy with the quality). That
prompted his cost principle → banked as memory [[prefer-cheaper-models]]: prefer the cheapest
model that holds quality, Opus reserved not default.

## Why the router change (49eb3c1)
The agent tool-use loop shifted the economics: the *tools* (search_vault/read_note/web_search)
do the heavy lifting now, and the model mostly orchestrates + writes — which Sonnet does well.
So Opus-on-every-research-query was unjustified. Flipped Research → Sonnet, and added a `Deep`
mode (Opus) that's the ONLY path to the frontier model, reached by explicit escalation only
("go deep / think hard / be thorough / use opus"). The cheap classifier tie-break can't return
Deep, so Opus is never reached by reflex — exactly Tucker's "less Opus when quality holds."

## Open threads
- [ ] Watch research quality on Sonnet across real tasks — if it slips, the escalation phrases
      (or a smarter Deep trigger) are the dial.
- [ ] Deferred agent-UX pass still pending (separate thinking from answer, fade+collapse) — [[roadmap]].

### 08:53 — 49eb3c1
Router: research → Sonnet, Opus reserved to an explicit "deep" mode
files: knowledge/journal/2026-06-01-agent-loop.md, knowledge/wiki/build-status.md, src-tauri/src/router.rs

### 09:01 — 316497b
Journal: router cost-tuning rationale (research→Sonnet, Deep=Opus)
files: knowledge/journal/2026-06-02-session.md

## Native macOS companion behaviors (built) + design direction captured
Tucker greenlit #1 and added three threads.

### Built: tray + dock toggle + close-to-hide
- **Tray icon** (menu bar): `tray-icon`+`image-png` features; `src-tauri/icons/tray.png` (44px
  template from `assets/brand/amber-menu.png`, `icon_as_template(true)` so it adapts to light/dark).
  Left-click opens the main window; menu = Open Amber / Command Bar (⌥Space) / Quit.
- **Dock-icon toggle** (Settings): `dock.hidden` config file + `get/set_dock_hidden`; applies
  `ActivationPolicy::Accessory|Regular` live and at launch. `AppHandle::set_activation_policy`
  exists in Tauri 2 (compiled clean). Default = dock shown.
- **Close-to-hide**: `on_window_event` CloseRequested on `main` → `prevent_close()` + hide; app
  lives in the tray (real quit = tray → Quit). These three = the Raycast-companion shell.

### Captured (not built) — design direction
- **Raycast window unification** (Tucker, this turn): collapse the two-window model into ONE
  frameless morphing surface — ⌥Space bar that EXPANDS into the full chat surface (conversations +
  journal + features), no traffic-light window. Implies conversation persistence (new) + expanded
  layout. Design-led — Tucker is designing it in Figma. Logged in [[design]] + [[roadmap]];
  supersedes M3's two-window model.
- **Figma file** started ("Amber — Desktop Agent", blank for now) → canonical UI source of truth.
  Documented in [[design]] + memory `amber-figma-file`. Build replicates from Figma going forward.
- **Vault write-back testing**: Tucker will duplicate his FOUNDRY vault to a safe sandbox before
  any write touches the live vault. Noted on the write-back roadmap item.

### Open threads
- [ ] Live-verify the tray (icon shows, left-click opens, menu works), the dock toggle (flips
      live), and close-to-hide (window closes → app stays in tray).
- [ ] Next strategic leap: vault write-back (the learning loop) — needs the sandbox + a design huddle.
- [ ] Then the Raycast morph, driven by Tucker's Figma.

### 09:27 — 5a845e0
Native macOS companion shell: tray icon + dock toggle + close-to-hide
files: knowledge/journal/2026-06-02-session.md, knowledge/wiki/build-status.md, knowledge/wiki/design.md, knowledge/wiki/roadmap.md, src-tauri/Cargo.lock, src-tauri/Cargo.toml, src-tauri/icons/tray.png, src-tauri/src/lib.rs, src/App.css, src/App.tsx

---

# 🛑 SESSION HANDOFF — pick up here next time (2026-06-02 EOD)

## Where the build is
Amber is now a real agent behind a beautiful surface. Done + on `main`:
- **M0–M2** (scaffold, streaming OpenRouter chat, vault retrieval) — done, verified.
- **M3** command bar (⌥Space palette) — done, verified.
- **M4** task routing — done; cost-tuned this session (**research → Sonnet; Opus only via the explicit `Deep` mode**, see [[prefer-cheaper-models]] memory).
- **Agent runtime / the spine** ([[journal/2026-06-01-agent-loop]]) — model-driven tool-use loop: `search_vault` / `read_note` / `web_search`. **Verified live** (web_search bug fixed — it now harvests real OpenRouter `annotations`, not the sub-model's stale prose). Stop button added (⌥ the ■ in composer/palette).
- **Native macOS shell** (this session, `5a845e0`) — tray icon + dock-icon toggle (Settings) + close-to-hide. **Pending Tucker's live confirm.**

## ⚠️ First thing next session — live-verify the native shell (5a845e0)
The dev app was left running. Confirm:
1. Tray ember icon in the menu bar; left-click opens main; menu (Open / Command Bar / Quit) works.
2. Settings → "Hide dock icon" flips the dock live; toggle back restores it.
3. Closing the main window hides it (app stays in tray); reopen via tray or ⌥Space.
Then flip "Native macOS shell" to ✅ in [[build-status]]. Also worth an eyeball: the M4 label now reads `sonnet 4.6 · research` normally, `opus 4.8 · deep` when you say "go deep".

## Next moves, in priority order (Tucker-agreed)
1. **Vault write-back — the learning loop** (the big north-star gap; vault is read-only today). BLOCKED on two prereqs Tucker owns: (a) **duplicate the FOUNDRY vault to a safe sandbox** to point Amber at, and (b) a **short design huddle** — what writes, where, what format (frontmatter + wikilinks, opt-in, never silent). It becomes another tool on the agent loop. See [[roadmap]] "Vault write-back".
2. **Raycast window unification** — collapse the two-window model into ONE frameless surface that morphs: ⌥Space bar → expands into the full chat surface (conversations list + journal + features), no traffic-light window. **Design-led** — driven by Tucker's Figma ([[design]]), which is currently blank. Implies new infra: **conversation persistence**. Build *from* the Figma frames.
3. **Agent-UX polish** — separate thinking from the answer + fade/collapse the step trail; visual-hierarchy pass. Likely folds into #2's Figma work.

## Key context to re-load (read these first next session)
- [[north-star]] — the canonical vision (agentic backend, beautiful surface, Hermes/OpenClaw/Pi-class).
- [[design]] — Raycast window direction + the **Figma file** (UI source of truth; build replicates from it).
- [[build-status]] — milestone tracker. [[roadmap]] — the parking lot (agentic-runtime arc).
- Memory: [[prefer-cheaper-models]], `amber-figma-file`.

## Housekeeping
- A `tauri dev` instance is still running in the background (holds ⌥Space). Quit it via the new tray → Quit, or just let it be.
- All work through `5a845e0` is committed and pushed to `main`. Repo is clean; gitleaks hook active.

_Status: paused — clean stopping point. Nothing half-finished in code._
