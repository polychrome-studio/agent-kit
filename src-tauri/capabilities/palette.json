{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "palette",
  "description": "The floating Raycast-style command bar window. It hides itself on Esc / blur; the global shortcut shows it (handled in Rust).",
  "windows": ["palette"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "core:window:allow-show",
    "core:window:allow-set-focus"
  ]
}
